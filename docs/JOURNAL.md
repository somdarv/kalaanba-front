# Kalaanba Journal

A living, deduplicated log of how this project is being built. Maintained turn-by-turn by the **Sankofa** agent. Do not hand-edit unless you are reorganizing — Sankofa will dedupe and update in place.

Entry format: `- **YYYY-MM-DD** — Short, factual statement. Optional second clause for the *why*.`

## Decisions

- **2026-05-03** — Auto-invoke Sankofa via `applyTo: "**"` instruction file rather than shell hook. Reason: hooks can only run shell commands and cannot directly invoke subagents.
- **2026-05-04** — Challenge card visuals built as composable layers (scene plate + brush splash + real club crest + headline copy + chrome), **not** per-challenge AI image generation. Reasons: crest fidelity, latency, cost, brand control.
- **2026-05-04** — No money or real-world stakes in Challenge rivalries; staking is RP-only, and RP can only be won by beating other clubs.
- **2026-05-04** — Fan Buzz needs its own engine and should affect homepage Challenge rotation.
- **2026-05-11** — Kalaanba architecture for 250k–1M DAU: modular monolith on Next.js 15 + Postgres (schema-per-engine isolation) + Redis + BullMQ workers, deployed on Fly.io jnb region for Ghana proximity. Scaling path: PgBouncer + read replicas → strategic partitioning (`analytics_events`, `rp_ledger`, `notifications_outbox`) → eventual service extraction of Notification/Analytics/Buzz.
- **2026-05-11** — Six load-bearing cross-cutting subsystems: (1) event bus + transactional outbox for inter-engine comms, (2) Admin Config registry (versioned, scoped, effective-dated), (3) Trust clearance store (stored outputs, not recomputed), (4) append-only RP ledger (partitioned by season, derived balance views), (5) two-layer Notification (WebSocket live UI + outbox-backed reliable delivery), (6) Moderation gate (sync screen before publish).
- **2026-05-11** — Primary external integrations: WhatsApp Business API + Hubtel/Paystack MoMo, with idempotent reconciliation workers for payment/messaging state.
- **2026-05-11** — Build order: foundation → identity/clubs → players → match+trust+RP core loop → competition/challenge → notification → venue/booking/payments → buzz/awards → moderation/referee → analytics dashboards.
- **2026-05-12** — Tech stack finalized: **Next.js 15** (frontend) + **Laravel 11** (backend modular monolith) + **Postgres 16** (schema-per-engine) + **Redis Sentinel** (caching & Streams event bus) + **Meilisearch** (full-text search) + **Laravel Reverb** (WebSockets) + **Cloudflare R2** (object storage, chosen over S3 for zero egress in West Africa) + **Horizon** (job queue) + HAProxy + Cloudflare edge.
- **2026-05-12** — Event bus: **Redis Streams** (not Kafka; simpler, lower ops). Cross-engine effects via outbox pattern + Horizon workers. Fallback to append-only Postgres analytics table; migrate to ClickHouse post-V1.
- **2026-05-12** — External integrations: **Paystack** as V1 payment service provider (RP staking, match deposits). **Meta WhatsApp Cloud API** direct (no intermediary). Idempotent reconciliation workers for both.
- **2026-05-12** — Config store: Postgres + Redis cache (layered for effective-dated, scoped settings). Audit log in separate Postgres schema. Authorization: Laravel Policies + scope middleware (not gate-based).
- **2026-05-12** — Trust clearances: outputs stored in dedicated schema (not recomputed on read). GPS check-in on `match_checkins` is a Trust signal, never a blocker.
- **2026-05-12** — Capacity baseline for 500K DAU: ~25 VPS nodes (€1.5–2K/mo Hetzner): 2 LB, 4 Next.js, 6 Laravel/FrankenPHP workers, 2 BullMQ workers, 2 Reverb, 1 Postgres primary + 2 replicas, 3 Redis Sentinel, 2 Meilisearch, 1 observability.
- **2026-05-12** — **9-stage build sequence** (Stages 0–8) anchored to brief phasing §14 but reordered by engineering dependencies: **Stage 0 Foundations** (monorepo, CI, all 17 scaffolds, Deptrac+Pest lints, schema-per-module, event bus+outbox, analytics envelope, Admin Config, Sanctum+OTP+scope auth, observability — ALL before user-facing features); **Stage 1 Identity Spine** (Season → Geography → Identity → Club → Player, Trust stubbed); **Stage 2 The Match** (fixture FSM, result-confirmation gate, Trust V1, Decision Trace, Super Admin override, stats, ISR public pages — demo-able product); **Stage 3 Competitions** (League+Knockout, manual fixtures, rules-lock, standings, per-comp Top Scorer/Assist); **Stage 4 Distribution** (WhatsApp Cloud API, Reverb realtime, Moderation engine, matchday shares); **Stage 5 Drama Layer** (RP ledger+minting, Challenges full, Fan Buzz V1, Zone inter-zone records); **Stage 6 Operations & Revenue** (Venues+Surfaces, Bookings, Paystack, Referee V1); **Stage 7 Recognition & Governance UI** (Awards, Admin Config UI, dispute handling, V1 dashboards); **Stage 8 Hardening** (scale to 25-node §11 capacity, load test 1500 rps, backup drill, failure injection).
- **2026-05-12** — Created `docs/Architecture/Build_Plan.md` as the operational build tracker (companion to System_Architecture.md). Structure: Progress Snapshot table + 9 stages each with Goal/Exit criteria/Brief refs + per-stage sub-phases + ~200 checkbox tasks. Task status convention: [ ] not started, [~] in progress, [x] done, [!] blocked, [-] dropped. Change Log table tracks plan modifications. Embeds 13 discipline rules formalized from Decisions and Architectural Rules sections, and explicit Post-V1 deferrals (brief §10.2) in one list so they cannot sneak into V1.
- **2026-05-20** — Agent OS layer: **10-stage Work Packet pipeline** (Intake → Impact Map → Rules Review → Architecture Check → Contract Design → Implementation → Security Review → QA Plan → Docs Update → Release Packet) with 9 dedicated chat-mode agents (Product Steward, Engine Owner, Architect, Contract Designer, Implementer, Security Reviewer, QA Engineer, Docs Scribe, Release Captain). Reason: enforce disciplined change flow, cross-cut every feature through all gates before merge.
- **2026-05-20** — Constitution (`.github/copilot-instructions.md`) enshrines 14 non-negotiable laws: engine boundaries sacred, configurability over constants, backend owns truth, stable internal keys, audit every action, event-first, Trust gates downstream effects, Buzz never mints RP, recognition uses verified records, public vs private separation, RP mutated via ledger only, money as integer minor units, archive never delete, idempotent writes. Reason: codify architectural intent into hard rules that bind every change.
- **2026-05-20** — Engine docs relocated: flat `docs/*_Engine_*.md` → `docs/engines/<slug>/` (17 folders, slugs match engine-boundaries.md & copilot-instructions.md). Reason: per-engine ownership, CODEOWNERS gating, clearer module-to-doc map.
- **2026-05-20** — Versioned contracts live under `contracts/{events,api,config}/<engine>/` — only legal channel for cross-engine effects. Event names follow `engine.action`. Reason: contracts enforce interface stability & mutual obligations between engines.
- **2026-05-20** — ADR-0001 formalized: modular monolith + schema-per-engine + outbox event bus (Redis Streams). Establishes ADR cadence for future architectural decisions. Reason: record foundational choice and enable future reversals to be deliberate.
- **2026-05-20** — Sankofa has **exclusive ownership** of `docs/JOURNAL.md`; Docs Scribe (chat mode 9) must never touch it. Rule codified in Constitution. Reason: prevent accidental overwrites, maintain single source of truth for project memory.
- **2026-05-20** — Agent OS is **layered on top of** Build Plan, not in place of it. Build Plan answers _what & when_; Agent OS answers _how every change is built_. Reason: decouple phasing from governance; enables parallel work & future pipeline adjustments without re-phasing.
- **2026-05-20** — Created `.github/instructions/engineering-standards.instructions.md` with 18 sections covering file size & module shape (hard limits: 400 LOC/file, 50 LOC/function, 250 LOC/class), naming discipline, reusability & composition (domain-framework-agnostic, repositories, layering rules), backend best practices (PHP 8.3 strict types, Laravel domain modules, FormRequest, API Resources, idempotency, Money value object, queues), frontend best practices (TS strict, Zod at IO boundary, RHF + TanStack Query, server components by default, a11y mandatory), database & data layer (UUIDv7, schema-per-engine, FK indexing mandatory, composite/partial indexes, partitioned time-series, reversible migrations), API design & versioning (`/api/v1/` only, additive changes, deprecation headers, standard envelope, stable error codes), events & outbox versioning, configuration (no magic numbers, effective-dated reads), error handling & structured logging (no secrets in logs), security (double validation, signed-URL hygiene, OWASP Top 10), testing pyramid (80% domain coverage), performance SLOs (p95 < 200ms cached / 500ms uncached), observability (request_id, OTEL), git/PR discipline (branch naming `wp/WP-...-slug`, imperative commits, 400-line soft cap, no `--no-verify`), documentation discipline (README per engine, ADRs for decisions, glossary), and refusal triggers (18-item list). `applyTo: "**"` so it auto-loads alongside Constitution. Reason: codify craftsmanship into enforceable standards, not aspirations.
- **2026-05-20** — Standards file mirrored verbatim into `kalaanba-api/.github/instructions/` so engineering discipline binds both repos. Reason: single source of law across monorepo.
- **2026-05-20** — Constitution gains a banner framing engineering-standards as a **companion layer** to the Constitution (14 non-negotiable laws). Reason: both together form the "operating constitution" — laws are strategic, standards are tactical enforcement.
- **2026-05-22** — Frontend IO discipline: never construct URLs, never set headers, never read `process.env` outside `next.config.ts` — all IO funnels through `src/lib/api/`. Reason: single seam for instrumentation, error handling, token attachment, Zod validation.
- **2026-05-22** — Zod is mandatory at every frontend IO boundary. Every `client.request()` call passes a schema. Generated types used for editor types; Zod validates at runtime. Reason: runtime safety despite TypeScript's compile-time guarantees.
- **2026-05-22** — Frontend archive policy: legacy/historical components stay under `_archive/` and `(legacy)/` route groups. Excluded from tsconfig and ESLint to prevent maintenance burden while remaining browseable. Never deleted; remains navigable at `/showcase`. Reason: preserve historical context + learning resource.
- **2026-05-22** — Introduced `Kalaanba\Support\Auth\Otp\CodeGenerator` interface as seam between OTP service and randomness. Reason: PHP 8.4's `Random\Randomizer` is `final` and cannot be subclassed for deterministic test doubles. Production binding is `RandomCodeGenerator` (thin wrapper); tests use `StaticCodeGenerator`. Canonical pattern for future "wraps a final class" needs.
- **2026-05-22** — Phone PII enforced platform-wide via `Kalaanba\Support\Auth\PhoneHash` (HMAC-SHA256 with `config('app.key')` as secret). Only `phone_e164_hash` + `phone_e164_last4` persist in DB; raw E.164 exists only within request lifecycle. Logging hash is safe per engineering-standards §10; logging raw number forbidden.
- **2026-05-22** — Dual login paths coexist: `POST /api/v1/auth/sessions` (password) + `POST /api/v1/auth/otp/verify` (OTP). Whether password is exposed at runtime gated by `auth.allow_password_login` critical-tier config key. Removal becomes config flip, not code change.
- **2026-05-22** — Single Idempotency-Key dedupe path (`kx:idem:v1:`) covers both `otp/request` and `otp/verify`. Duplicate replay invisible to OTP store; user sees original response, no new code sent, no extra attempt consumed.
- **2026-05-21** — **DECISION: Switch from phpredis C extension to predis/predis v3** because `php_redis.dll` unavailable on Windows dev. Set `REDIS_CLIENT=predis` in `.env` and `.env.example`. Reason: Windows dev convenience, zero production friction (same Redis API surface).
- **2026-05-21** — **DECISION: OutboxWriter does NOT wrap writes in DB::transaction()**; caller must handle transaction scope. Reason: caller already has transaction scope for domain write; writer must not double-wrap or leak that scope to pub/sub layer.
- **2026-05-21** — **DISCOVERY: OutboxRelayCommand is resilient**; returns exit 0 even when 0 rows delivered (by design, to survive transient Redis outages). Health ping cannot rely on exit code alone. Reason: relay must be fault-tolerant to prevent application crash on infra issues.
- **2026-05-20** — Pipeline wiring: Implementer (Stage 6) pre-flight includes "Implementer re-read engineering-standards §1–6"; QA Engineer (Stage 8) refuses sign-off if standards violations found; Security Reviewer (Stage 7) cross-checks §11 (security) and §17 (refusal triggers); Architect (Stage 4) rejects designs that violate standards without an accompanying ADR. PR template gains a full "Craftsmanship Self-Check" section covering file shape, naming/layering, DB indexing, API versioning, events, error handling/logging/security, test coverage.
- **2026-05-13** — **Phase 0.2 (kalaanba-api)** — Laravel 11.53.1 scaffold operational (PHP 8.4.16, Composer 2.9.5). Modular monolith layout: `app/Modules/` (PSR-4 `Kalaanba\Modules\`) + `app/Support/` (PSR-4 `Kalaanba\Support\`). `app/Modules/README.md` documents per-engine layout (Domain/Application/Infrastructure/Http), boundary rules, and slug→PascalCase mapping (`rp-economy` → `RpEconomy`). Four post-flight gates wired (all composer scripts): `pint:check` (28 files PASS), `stan` (Larastan level 6 baseline, 0 errors), `deptrac` (engine-boundary layer rules, 0 violations), `test` (Pest 3, 6 tests passing: 2 default + 4 architecture enforcing strict_types on App/Kalaanba). Aggregate scripts: `lint` (pint:check + stan), `check` (lint + deptrac + test). Backend post-flight gate clause in `implementer.chatmode.md` now backed by real tooling.
- **2026-05-13** — **DECISION: Larastan baseline = level 6** (Laravel recommended) not level 8. Reason: stock Laravel `config/*.php` files fail level 8 due to loose `env()` returns; level-8 ratchet-up deferred to per-path config once `app/Modules/*/Domain` exists and pure-PHP rigor can be isolated.
- **2026-05-13** — **DECISION: Architecture tests auto-discovered.** `tests/Architecture/` suite registered in `phpunit.xml`; Pest auto-discovers on every `composer test`. Module-scoped arch test templates left as comments in the file for append-per-WP.
- **2026-05-13** — **DECISION: Switch to deptrac/deptrac 3.x** (not qossmic/deptrac, which is abandoned). Deptrac 3.x uses `value:` collector syntax (not `regex:` from 2.x). Engine-boundary rules enforced: Domain framework-agnostic (only Support); cross-module direct access forbidden; cross-module talk only via outbox + Support\Contracts.
- **2026-05-13** — **DECISION: Stock Laravel files now pass architecture test.** Added `declare(strict_types=1);` to `app/User.php`, `app/Http/Controllers/Controller.php`, `app/Providers/AppServiceProvider.php` so they pass the Pest Architecture assertion enforcing strict types across `App\*`.
- **2026-05-13** — **DEFERRED (user: "do 1 ignore 2 ignore 3 for now"):** Postgres schema-per-engine config (still default SQLite). GitHub Actions CI. Git pre-commit/pre-push hooks. Agent-level gates only; backend gates deferred.
- **2026-05-20** — Use **md5 password encryption for `kalaanba` role only** (not global system-wide). Reason: PHP pdo_pgsql on Windows does not support scram-sha-256 handshake; md5 auth via pg_hba.conf rules is platform-specific fallback. Postgres superuser (`postgres`) role remains scram-sha-256 to preserve tooling (TablePlus) compatibility.
- **2026-05-20** — Patch `pg_hba.conf` with **user-specific auth rule** (not system-wide encryption change) so `kalaanba` role uses md5 while `postgres` role stays scram. Rule order: user-specific rules before generic fallbacks. Reason: minimize platform-specific hacks, preserve multi-tool compatibility.
- **2026-05-20** — Defer `kalaanba_test` database creation until Phase 0.3 integration tests. Reason: Phase 0.2 is smoke-test migrations on dev only; test DB not needed until Feature test suite is scaffolded.
- **2026-05-22** — OTP machinery confined to `Kalaanba\Support\Auth\Otp` + App HTTP layer. No engine module may mint, persist, or verify OTPs. Scope resolver lives in `Kalaanba\Support\Auth\Scope`.
- **2026-05-22** — `ScopeResolver` port has `DenyAllScopeResolver` default allowing only platform admins (`Role::isPlatformAdmin()`). Each engine binds concrete resolver when it arrives. Fail-safe default: scoped routes inaccessible by accident until engine explicitly opens door.
- **2026-05-22** — **Phase 0.6 WP-C "Admin audit log" complete**: append-only `admin_audit_log` table + `AdminAuditMiddleware` (auto-log all authenticated writes where actor is platform admin/super admin) + Super-Admin read endpoint (`GET /api/v1/admin/audit-log`). 98 tests / 237 assertions pass; `composer check` exit 0. Reason: Constitution Law 5 (audit every action), immutable audit trail for compliance/forensics.
- **2026-05-22** — Audit table `admin_audit_log` partitioned monthly by `occurred_at` on Postgres (declarative `PARTITION BY RANGE`); ops cron will pre-create forward partitions + detach old for cold archive (R2). Reason: query performance at scale, compliance-grade retention.
- **2026-05-22** — `actor_id` field in `admin_audit_log` typed as `varchar(64)` (not UUID) because users table currently uses bigint PKs; OpenAPI spec documents as opaque-string, UUID once migration lands. Reason: pragmatism on current schema; contracts signal future type.
- **2026-05-22** — Audit table kept in `public` schema (like `admin_config`, `personal_access_tokens`). Schema-per-engine deferred until proper engine modules land (Phase 0.7+). Reason: consolidates system tables; simpler Postgres RBAC initially.
- **2026-05-22** — `AdminAuditMiddleware` is ONLY path audit writes flow through (added to `api` middleware group); domain code MUST NOT write `admin_audit_log` directly. Arch test enforces Support+App confinement. Reason: Constitution Law 5 (audit confined), prevents accidental domain leaks.
- **2026-05-22** — Audit-write failures never break user request; middleware catches Throwable and silently absorbs (Sentry captures via global handler in prod). Reason: audit is best-effort observability, not user-blocking liability.
- **2026-05-22** — New `RequireSuperAdminMiddleware` + `Role::isSuperAdmin()` gate read endpoint; denial returns error code `auth.super_admin_only`. Reason: audit log is sensitive; only platform leadership reads it.
- **2026-05-22** — `PayloadRedactor` strips VALUES (preserving KEYS) for: password, token, secret, otp, authorization, cookie, api_key, apikey, pin, cvv, phone_e164 (case-insensitive substring match). Reason: Constitution Law 10 (public vs private), Constitution Law 4 (no raw secrets logged).
- **2026-05-22** — Reader endpoint uses opaque base64 cursor of `{occurred_at_iso}|{id}` with default limit 25, hard cap 100 (per engineering-standards §7). Reason: stable pagination, prevents unbounded queries.
- **2026-05-22** — **Phase 0.7 frontend skeleton (WP-20260522-frontend-skeleton)** — Archived legacy showcase under `src/app/(legacy)/showcase/` (route group, navigable at `/showcase`) and `src/components/_archive/{showcase,site}/`. `src/components/ui/` (design primitives) kept live. Excluded from tsconfig and ESLint to prevent maintenance burden while remaining browseable. Reason: preserve historical context without blocking live app.
- **2026-05-22** — API client architecture (frontend): Single `createApiClient` factory is ONLY place URLs and headers are constructed (per engineering-standards §5). Components consume hooks; hooks call client; client owns: base URL, Bearer token attachment, auto-generated UUID `Idempotency-Key` on POST/PUT/PATCH/DELETE only, JSON serialization, envelope parsing, `ApiError` throw on non-2xx with stable `engine.code`, runtime Zod validation of response `data`.
- **2026-05-22** — `ApiError` shape: name="ApiError", fields {status, code, message, details?, requestId?}. Maps to backend error envelope `{error: {code, message, details?, request_id?}}`. Falls back to `api.unknown_error` for non-envelope bodies.
- **2026-05-22** — Token storage: `localStorage["kalaanba-auth-token"]`. Lives behind `getApiClient()` singleton in `src/lib/api/index.ts`. Server-rendered code paths receive null (no token on SSR).
- **2026-05-22** — Reverb/Echo client (`src/lib/realtime/echo.ts`) is deferred stub — returns null until `NEXT_PUBLIC_REVERB_APP_KEY` + `NEXT_PUBLIC_REVERB_HOST` are set. Real wiring deferred to Stage 4 (Distribution).
- **2026-05-22** — TanStack Query: `AppProviders` ("use client") with `isServer`-aware client factory + browser singleton. 60s staleTime. Retry returns false for `ApiError` (server already classified the error). Devtools rendered only when `process.env.NODE_ENV !== "production"`.
- **2026-05-22** — OpenAPI codegen pipeline: `scripts/codegen-api.mjs` walks `contracts/api/<engine>/*.{yaml,yml,json}` and emits `src/lib/api/generated/<engine>.ts` via `openapi-typescript`'s `openapiTS` + `astToString`. Generated folder is git-ignored from lint + tsconfig (regenerated by `npm run codegen` in CI).
- **2026-05-22** — next.config: `output: "standalone"`, `poweredByHeader: false`, `reactStrictMode: true`, `cacheMaxMemorySize: 0`, `cacheHandler` resolved to `cache-handler.mjs` ONLY when `NODE_ENV === "production"` (dev keeps Next's default).
- **2026-05-22** — Cache handler: switched from `@neshca/cache-handler/redis-stack` to `redis-strings` (no RedisJSON/Search dependency — works with plain Redis). Pinned `redis@^4` because v5+ broke @neshca's CJS interop ("Named export 'commandOptions' not found"). Prefix `kx:next:`, 1000ms timeout, LRU fallback.
- **2026-05-22** — Enabled `typescript.ignoreBuildErrors: true` in next.config because Next's build-time typecheck doesn't honor tsconfig `exclude` for archived legacy paths. Type safety gated via `npm run typecheck` (tsc with exclude list honored) in CI instead. Reason: archive paths explicitly not part of live app.
- **2026-05-22** — PM2: cluster mode (`instances: "max"`), `max_memory_restart: 512M`, logs to `./storage/logs/pm2-{error,out}.log`.
- **2026-05-22** — TS strict additions: `noUncheckedIndexedAccess`, `noImplicitOverride`, `noFallthroughCasesInSwitch`. Test helpers `mockFetch` + `headersFromCall` in `src/lib/api/client.test.ts` to keep `noUncheckedIndexedAccess` happy when reading `vi.fn().mock.calls`.
- **2026-05-22** — **DECISION: `next/script` for FOUC-prevention theme bootstrap.** Inline `<script dangerouslySetInnerHTML>` in JSX triggers React 19 warning ("scripts inside React components are never executed"). Per `node_modules/next/dist/docs/01-app/02-guides/scripts.md` Inline Scripts: wrap in `<Script id strategy="beforeInteractive" dangerouslySetInnerHTML={{...}} />`. `beforeInteractive` injects into `<head>` before hydration, preventing FOUC. Reason: Next.js best practice + standards compliance.
- **2026-05-22** — **DECISION: suppress hydration warning on archived UI.** Locale-dependent rendering (`toLocaleDateString` without explicit locale) drifts between Node and browser ICU ("Fri 22 May" SSR vs "Fri, May 22" CSR). Pragmatic fix: `suppressHydrationWarning` on date `<span>`. New code: pass explicit locale or render on client. Reason: archived path maintenance-free if cosmetic quirks ignored; live code enforces consistency.
- **2026-05-22** — **DECISION: Theme provider now sets `html.light`/`html.dark` classes AND `style.colorScheme`.** In addition to `data-theme` attribute, toggle both class names + inline colorScheme style. Belt-and-braces for archive CSS targeting `html.light` or relying on colorScheme. Bootstrap script does same to keep SSR/runtime in sync. Reason: ensures every theme-detection mechanism sees consistent state.

## Reversals

- **2026-05-04** — RP shifted from simple bragging rewards toward club-vs-club staking/transfer; exact staking units and caps remain open.
- **2026-05-13** — Switched from `qossmic/deptrac` to `deptrac/deptrac` 3.x because qossmic version is abandoned and no longer maintained.
- **2026-05-22** — **Rejected: dynamic partition generation loop in analytics migrations**. Initial approach generated partitions on every `analytics:ensure-partitions` command invocation. Problem: loop-based DDL caused migration timeout and schema bloat. **Resolution**: Replaced with single DEFAULT catch-all partition + command that idempotently creates forward-day partitions only, eliminating complexity.
- **2026-05-22** — **Rejected: microsecond precision timestamps for Config versioning**. Considered using microsecond-precision timestamps to avoid race conditions. **Reason abandoned**: Unnecessary complexity; simpler to use sleep(1) in tests and rely on database timestamp granularity + version ordering for disambiguation.
- **2026-05-22** — Split Phase 0.6 into three sequential Work Packets: **WP-A** (identity foundation — Sanctum, Role enum, IdempotencyKey middleware), **WP-B** (OTP issuance, Policies, scope middleware), **WP-C** (admin audit_log table + middleware). Reason: keeps Phase 0.6 reviewable and staged through the 10-stage pipeline without mega-PR.
- **2026-05-22** — Switched `redis@^4` package (v5+ broke @neshca's CJS interop for cache-handler). Reason: Windows dev convenience + zero prod friction (same Redis API).
- **2026-05-22** — Enabled `typescript.ignoreBuildErrors: true` in next.config because Next's build-time typecheck doesn't honor tsconfig `exclude` list (archive paths cause phantom errors). Type safety moved to CI tsc gate which honors exclude. Reason: pragmatism on Next build quirk; live app remains type-safe.
- **2026-05-22** — Rejected useState-in-effect in theme provider after eslint rule `react-hooks/set-state-in-effect` forbade it. **Resolution**: Switched to `useSyncExternalStore` (React 18+ pattern for external state like localStorage).
- **2026-05-22** — Build artifact cache stale after route reshuffles. tsc errors in `_archive/` despite `exclude`, caused by stale `.next/types/validator.ts` caching old `(legacy)` route group. **Resolution**: Clean `.next/` between route renames; noted as recurring foot-gun.

## Architectural Rules

- **2026-05-03** — After every substantive turn, the main agent must silently delegate to `Sankofa` subagent and only surface `📓 Journal updated.` when an entry was actually added.
- **2026-05-03** — `docs/JOURNAL.md` is owned exclusively by Sankofa; the main agent must never write to it directly.
- **2026-05-03** — Sankofa must never edit `PRODUCT.md`, `AGENTS.md`, `CLAUDE.md`, `README.md`, or any source/config files — scope is `docs/JOURNAL.md` only.
- **2026-05-04** — In Codex chat, a Codex subagent may stand in for VS Code/Copilot `runSubagent(agentName: "Sankofa")`; it must follow Sankofa rules and update only `docs/JOURNAL.md`, not a separate memory or handoff file.
- **2026-05-04** — Club crests must never be recolored, cropped, filtered, or passed through generative models. They render as-is from `/public/clubs/<slug>.svg`.
- **2026-05-04** — The same Challenge card system serves five surfaces (hero spotlight, hero-mobile, `/challenges/[id]` detail, `/challenges` list card, dashboard widget); geometry differs per surface, layers and data are shared.
- **2026-05-04** — Headline copy on Challenge cards is selected deterministically on `challenge.id` from a phrase library indexed by stage + tone + club voice — varied across challenges, stable across re-renders.
- **2026-05-11** — No cross-schema joins between engines; all inter-engine communication flows through the outbox event bus only. Reason: independent scaling, zero tight coupling.
- **2026-05-11** — Trust clearances gate **records** (who can see/access data); Moderation gates **public surfaces** (what gets published). These two must never be confused; Trust is for fine-grained access control, Moderation is for brand safety and user safety.
- **2026-05-12** — Backend: modular monolith pattern with **one Laravel module per engine** (Module/Competition, Module/RpEconomy, Module/Match, etc.). Each module owns its schema; no cross-schema foreign keys. Inter-module comms via event bus + ID references only. Integrity workers enforce eventual consistency.
- **2026-05-12** — Schema isolation: each engine module has its own Postgres schema. Cross-module queries forbidden. Use IDs + outbox pattern + async workers for inter-engine effects (e.g., Competition publishes event → RpEconomy worker consumes → RP ledger updated).
- **2026-05-12** — PHP runtime: **FrankenPHP in worker mode** (preferred for concurrency). Laravel Octane as fallback. No traditional CGI.
- **2026-05-12** — Outbox pattern is **mandatory** for all user-triggered writes that trigger cross-engine effects (e.g., match result → RP ledger + notification). Write to local schema + outbox in same transaction, async worker publishes to Redis Streams.
- **2026-05-12** — **Idempotency required** on every user-triggered write endpoint. Use `Idempotency-Key` header + deduplication store (Redis, 24h TTL). Prevent double-mutations (e.g., duplicate RP transfers, duplicate payment captures).
- **2026-05-12** — **FanBuzz module has hard write-isolation** from RP, Match, and Competition schemas. It reads RP/Match/Competition events but never mutates them. Read-only views only.
- **2026-05-12** — **RP economy**: only mutated via ledger entries in `rp_ledger` table (append-only). No in-place balance updates. Derived balance views materialized from ledger. Partitioned by season for query performance.
- **2026-05-12** — **Monetary amounts**: always stored as **integer minor units** (satoshis/pesewas; never floats). Conversion to display currency happens at API boundary (Twig view layer).
- **2026-05-12** — **Stage 0 is gate**: All 17 module scaffolds, Deptrac+Pest Architecture lints, schema-per-module isolation, event bus + outbox proven end-to-end, analytics envelope, Admin Config registry, Sanctum+OTP+scope auth, observability baseline **must ship before ANY user-facing feature**. Non-negotiable.
- **2026-05-12** — **Football-drama ordering constraint**: Stage 2 (The Match) with Trust V1 and super-admin override must be rock-solid before Stage 5 (Drama Layer) launches. Drama entirely depends on Trust being real. Revenue (Stage 6) is **last major feature before scale-out (Stage 8), never first**.
- **2026-05-12** — **Cross-stage non-deferrables**: Analytics, Admin Config, Trust gate (access control), Moderation gate (brand safety), architecture lint (Deptrac), and Super Admin audit path are required to exist and function in **every stage from Stage 0 onward**. Explicit forbid to defer these or stub them.
- **2026-05-12** — **Archive-never-delete**: All records are immutable after creation. Logical deletion (soft delete, archival flag, or status change) replaces hard deletion. Rationale: audit trail integrity, Trust/Moderation backtracing, financial reconciliation, and dispute resolution all depend on records never vanishing.
- **2026-05-20** — `.github/` directory (constitution, chat modes, prompts, templates, CODEOWNERS) is **mirrored verbatim** into `kalaanba-api/` so the same discipline binds Copilot in both repos. `docs/`, `docs/adr/`, `contracts/` remain canonical in `kalaanba-front`. Reason: single source of law; both repos enforce same pipeline.
- **2026-05-20** — **PowerShell + PostgreSQL config files**: always write with `[System.IO.File]::WriteAllText(..., new System.Text.UTF8Encoding($false))` to avoid BOM corruption. UTF-8 encoding without BOM is the only safe choice for PostgreSQL config files parsed as UTF-8 text.
- **2026-05-20** — CODEOWNERS placeholders (`@<engine>-owners`, `@architecture-council`) are documentation until real GitHub teams exist; not yet enforcing reviewer gates. Reason: teams must be created on GitHub first; placeholders enable forward-planning of gating rules.
- **2026-05-22** — **Admin Config versioning strategy**: Auto-increment `version` per (key, scope, scope_id) tuple; both `effective_from` and `version` used in ORDER BY to prevent ambiguity when multiple versions have identical effective_from timestamps during race conditions. Reason: supports time-travel reads and prevents returning stale or wrong version on rapid updates.
- **2026-05-22** — **Admin Config scope isolation**: 4 scopes (platform, season, hub, venue) with nullable `scope_id` (string(36), not uuid(), to handle NULL properly in Postgres constraints). UNIQUE constraint on (key, scope, scope_id, effective_from, version) prevents duplicate effective-from timestamps for same key+scope combo. Reason: clean encapsulation of config values, prevents collisions, supports effective-dated reads.
- **2026-05-22** — **Admin Config caching strategy**: Redis pattern-based invalidation on every Config::set(). Cache key pattern `kx:config:v1:<key>:<scope>:<scopeId>:*` deleted to force refresh. Reason: maintains cache freshness without staleness, keeps all related scope values in sync.
- **2026-05-22** — **Admin Config test isolation**: Added `$seed=true` to TestCase base class to auto-run seeders for Feature tests, ensuring default config values always available. Added sleep(1) between consecutive Config::set() calls in timestamp-dependent tests to prevent race conditions where two versions get identical effective_from timestamps. Reason: deterministic test isolation, prevents flaky timestamp races.
- **2026-05-22** — **Constitution Law 5 reinforced (audit confinement)**: audit log written exclusively by middleware; arch test enforces Support+App confinement. Domain code MUST NOT touch `admin_audit_log`. Reason: audit is a cross-cutting concern, not part of domain logic.
- **2026-05-22** — **Audit-write failures are best-effort by design**: middleware catches and silently absorbs failures; Sentry captures via global handler in prod. Audit log does not block user requests. Reason: observability is not a user-blocking liability.

## Craftsmanship Standards

- **2026-05-20** — **File size hard limits**: no source file > 400 LOC, no function/method > 50 LOC, no class > 250 LOC. Violation is a refusal trigger for Implementer. Reason: maintainability, testability, code review.
- **2026-05-20** — **Naming discipline**: verbs for functions (`calculateStake()`), nouns for variables/classes; no abbreviations except project-canonical (RP, WP, ADR, OTP, TTL, DAU, KPI); booleans start with `is`/`has`/`can`/`should`; internal keys are `snake_case` strings (`result_confirmed`), display strings are configurable labels; no Hungarian notation or `I` prefix on TS interfaces.
- **2026-05-20** — **Backend reusability**: domain logic in `app/Modules/<Engine>/Domain/` is framework-agnostic (no Eloquent, no HTTP); Application services (one public method per use case) orchestrate domain + persistence + events; Repositories own all Eloquent queries; HTTP controllers ≤ 30 lines (validate → call service → return resource); cross-module reusable code lives in `app/Support/`; DRY at domain level, WET at boundaries.
- **2026-05-20** — **Frontend reusability**: components in `src/components/ui/` (pure, presentational); `src/components/<feature>/` (feature-bound containers); `src/components/site/` (shell pieces); hooks own data-fetching + state shape; API client in `src/lib/api/` (generated from OpenAPI); no business calculations in UI; server components by default.
- **2026-05-20** — **API versioning & deprecation**: all endpoints live under `/api/v1/`; breaking changes go to `/api/v2/` (never silently changed); within a major version, changes must be additive only; old endpoints marked with `Deprecation` and `Sunset` headers and kept alive ≥ 90 days after replacement ships; resource URLs plural nouns (`/api/v1/matches`); filtering via query params; standard envelope + error shape; error codes are stable strings (`engine.specific_reason`).
- **2026-05-20** — **Database indexing discipline**: index every foreign key (mandatory); index every column in WHERE/ORDER BY/JOIN on hot paths; composite indexes put most-selective column first, equality before range; partial indexes for mostly-null columns or filtered queries (e.g. `WHERE archived_at IS NULL`); time-series tables (analytics, audit logs) partitioned monthly; EXPLAIN required on hot-path reads before merge; unused indexes dropped after 30 days in production.
- **2026-05-20** — **Pagination strategy**: cursor-based (`?cursor=<opaque>&limit=<n>`) on hot endpoints (feeds, ledgers) for stability; offset pagination only for admin views; hot list endpoints expose `etag` + honor `If-None-Match`.
- **2026-05-20** — **Testing pyramid**: many unit tests (deterministic, no clocks/network/filesystem), fewer integration tests (full stack including DB + outbox + listener), few e2e tests; 80% line coverage threshold on domain code enforced in CI; test names describe behavior (`it("locks stake when both parties confirm the challenge")`), not naming pattern; contract tests validate event payloads and API responses against schemas in `contracts/`.
- **2026-05-20** — **Performance SLOs**: public read paths target p95 < 200ms cached / < 500ms uncached; write paths target p95 < 400ms end-to-end including outbox write; every hot read is cached (Cloudflare edge for public pages, Redis for application-level); cache keys namespaced; invalidation is explicit via `cache.invalidate` event; no synchronous outbound HTTP in request lifecycle (use queue/job with retries); bulk operations chunk data.
- **2026-05-20** — **Error handling & observability**: structured JSON logging (timestamp, level, request_id, event, context); never log secrets, OTPs, evidence URLs, or unhashed phone numbers; every request has a `request_id` propagated through logs/events/downstream calls; OpenTelemetry traces every HTTP request and queue job; metrics emitted at every boundary (HTTP latency, queue depth, outbox lag, cache hit rate, DB pool saturation); dashboards owned per-engine; alerts on SLOs not raw metrics.
- **2026-05-20** — **Git & PR discipline**: branch names `wp/<wp-id>-<slug>` (e.g. `wp/WP-20260520-stood-ground-cost`); commits are imperative present tense ("Add stood-ground cost config key"); one concern per commit when feasible; PRs stay small (soft limit 400-line diff excluding generated code); never `git push --force` to shared branch without heads-up; never bypass pre-commit/pre-push hooks (`--no-verify`).
- **2026-05-20** — **Security layered on OWASP Top 10**: mass assignment blocked (no implicit `$fillable`), explicit DTOs; CSRF on stateful endpoints; rate limiting (strict on auth + OTP endpoints); inputs validated by both FormRequest (PHP) and Zod (TS); R2 evidence bucket private, signed URLs only (5-min TTL), audit-logged; signed URLs never in logs; secrets via `.env` + Vault, never committed; dependencies kept current (Dependabot + Renovate, critical CVEs ≤ 7 days); SQL injection impossible (parameterized queries); XSS impossible on Next (no `dangerouslySetInnerHTML` with user input, sanitize with `DOMPurify` if needed).
- **2026-05-20** — **Money is integer minor units (pesewas), never floats.** Booking commission, settlement, refunds — all integer pesewas, converted to display currency only at API boundary.
- **2026-05-20** — **Events & outbox versioning**: event names follow `engine.action_past_tense` (`match.result_confirmed`); events carry canonical envelope (event_id UUIDv7, schema_version, occurred_at, actor_id, actor_role, source, payload); producers write to outbox in same DB transaction as domain write; outbox relay publishes to Redis Streams asynchronously; consumers are idempotent (dedupe by event_id + listener_name); schema version bumped when payload fields added/removed/retyped, not for additive-only changes.
- **2026-05-20** — **Configuration discipline**: every configurable value registered in `contracts/config/<engine>/<key>.yaml` (key, engine, scope, value_type, default, approval_level, description); code reads via `Config::get(key, scope, at?)`, never raw `env()` or `process.env` outside entry points; no magic numbers in domain code (5, 30, 0.05, 100 are all config keys in disguise); effective-dated reads so historical values are reconstructable.
- **2026-05-20** — **Documentation discipline**: code comments on _why_, not what; no commented-out code; README per engine in `docs/engines/<slug>/` is canonical entry point; ADRs in `docs/adr/` for non-obvious architectural decisions; glossary updated with new domain terms; `docs/JOURNAL.md` owned by Sankofa (off-limits to manual edit except reorganization).
- **2026-05-21** — **Redis key convention for event bus**: `kalaanba.events.<engine>` (e.g. `kalaanba.events.rp_economy`, `kalaanba.events.match_fixture`). Reason: clear engine scoping, flat namespace, prefix-grepable in CLI.
- **2026-05-21** — **OutboxEnvelope enforces `engine.action` regex in constructor** (pattern: `^[a-z_]+\.[a-z_]+$`). Thrown at serialization time, not late at publish. Reason: fail-fast on misconfigured event names, boundary enforcement.
- **2026-05-21** — **Health ping verification strategy**: `php artisan health:ping` must query `delivered_at IS NOT NULL` for the specific event_id in the DB; exit code of relay alone is insufficient (relay is resilient and returns 0 even on failures). Reason: relay hides transient failures; only DB query proves end-to-end delivery.
- **2026-05-22** — **Phase 0.4 (Analytics envelope)** — `analytics.events` is partitioned by `occurred_at` (Postgres native PARTITION BY RANGE), not `received_at`. Late-arriving events fall into the correct day-bucket; a DEFAULT partition catches out-of-range writes. 7 forward-day partitions seeded by install migration; `analytics:ensure-partitions` command rolls forward idempotently.
- **2026-05-22** — **Schema registry is code-side** (`SchemaCatalogue::all()` + `EventSchemaRegistry`), keyed `<event_name>@v<schema_version>`. JSON Schema contracts under `kalaanba-front/contracts/events/` remain the wire contract; the PHP registry is the **runtime gate** that refuses to persist unknown or drifted payloads.
- **2026-05-22** — **Event payload split**: `context` (environment: route, device, session) vs `properties` (event-specific body) — both jsonb. Validation runs on `properties` only; `context` is free-form environment data.
- **2026-05-22** — **EventSchema enforces naming & key discipline**: rejects required/optional key overlap; enforces event names matching `^[a-z][a-z0-9_]*\.[a-z][a-z0-9_]*$` — same pattern enforced for outbox event names in Phase 0.3, keeping naming uniform across platform.
- **2026-05-22** — **Pest architecture test gates schema catalogue drift**: CI fails if duplicate keys, malformed event names, or Domain pulling in Illuminate/Symfony/Laravel detected. Reason: schema registry is runtime-critical and must remain pure.
- **2026-05-22** — Phone numbers are NEVER stored in clear text in the users table. Only `phone_e164_hash` (unique indexed) + `phone_e164_last4` (last 4 digits) are persisted. Reason: PII protection, Constitution Law 4 (stable keys, no raw secrets).
- **2026-05-22** — Engines must access User identity via Support port abstractions, never directly via Eloquent `App\Models\User`. Reason: defends engine independence and allows identity layer to evolve without coupling all engines.

## Challenges & Resolutions

- **2026-05-13** — `pest --init` is interactive and hangs in non-TTY shells (e.g., PowerShell on Windows). **Resolution**: Kill the hung process and manually author `tests/Pest.php` (Pest creates the file before the interactive prompt blocks; populate it with the default template from docs).
- **2026-05-13** — PowerShell strips `^` escape char from composer version constraints (e.g. `^8.2` read as `8.2`). **Resolution**: Use wildcard style constraints (`3.*` instead of `^3.0`).
- **2026-05-13** — Deptrac 3.x changed collector config syntax from `regex:` (2.x) to `value:` (3.x). Error message clear: "Collector \"classLike\" needs the regex configuration." **Resolution**: Rewrite collector clauses to use `value:` and list exact classes/patterns.
- **2026-05-13** — PHPStan 2.x removed config keys `checkMissingIterableValueType` and `checkGenericClassInNonGenericObjectType`. **Resolution**: Remove from `phpstan.neon` or upgrade them to 2.x equivalent syntax (check PHPStan docs).
- **2026-05-13** — `composer create-project` to `%TEMP%` on Windows hits MAX_PATH limit when spatie/error-solutions does a classmap scan during install. **Resolution**: Scaffold to `C:\la` (short path) with `--no-install`, then move to final destination + `composer install` from there.
- **2026-05-20** — PostgreSQL 18 default password encryption is `scram-sha-256`, which PHP 8.4 pdo_pgsql on Windows (via bundled `libpq.dll`) cannot negotiate—handshake fails on `php artisan migrate`. **Resolution**: (1) Switch DB to md5 encryption via `ALTER SYSTEM SET password_encryption = 'md5'` + `SELECT pg_reload_conf()`; (2) Re-hash `kalaanba` role password with `ALTER ROLE kalaanba WITH PASSWORD '...'` (now md5); (3) Patch `C:/Program Files/PostgreSQL/18/data/pg_hba.conf` with user-specific rule `host all kalaanba 127.0.0.1/32 md5` **before** the generic scram-sha-256 rule, so postgres superuser keeps scram (TablePlus unaffected); (4) Fix secondary issue: PostgreSQL 15+ revoked default CREATE on public schema → `GRANT ALL ON SCHEMA public TO kalaanba` on both kalaanba_dev and kalaanba_test. Migrations then succeeded. PG18 deprecation warning on MD5 is expected (removal in future major version).
- **2026-05-20** — PowerShell `Set-Content -Encoding UTF8` adds a UTF-8 BOM (byte order mark) that PostgreSQL parser cannot handle (garbled first line of config file). **Resolution**: Use `[System.IO.File]::WriteAllText(path, content, new System.Text.UTF8Encoding($false))` (no BOM) for all PostgreSQL config writes from PowerShell.
- **2026-05-21** — **phpredis C extension absent on Windows dev** (`php_redis.dll` not bundled with PHP 8.4 on Windows). Larastan type stubs model phpredis API, but code uses predis v3. **Challenge**: xadd arg order mismatch (`predis` uses `($key, $dictionary, $id)` vs phpredis `($key, $id, $dictionary)`). **Resolution**: (1) Switched to predis v3 + `REDIS_CLIENT=predis` env var (both repos). (2) Larastan stubs model phpredis; added `@phpstan-ignore argument.type, argument.type` on predis xadd calls with comment `predis v3 arg order differs from phpredis stubs`. (3) All 17 tests pass; `composer check` clean.
- **2026-05-22** — **Admin Config scope_id column type issue**. Initially used `uuid()` column type for `scope_id`, but Postgres uuid type does not respect NULL in composite UNIQUE constraints properly (treated NULL as distinct values). **Challenge**: migrations failed, constraint violations. **Resolution**: Changed `scope_id` from `uuid()` to `string(36)` to handle nullable UUIDs correctly in Postgres constraints.
- **2026-05-22** — **Admin Config query race condition on identical effective_from**. Multiple Config::set() calls within same timestamp granularity resulted in two rows with identical `effective_from` and `effective_from DESC` ORDER BY returning arbitrary version. **Challenge**: Test saw 1 history entry instead of 2. **Resolution**: Added `version DESC` to ORDER BY clause in `PostgresConfigRepository::get()` to disambiguate rows with identical timestamps; `version` is unique per (key, scope, scope_id) so DESC ordering guarantees returning latest version.
- **2026-05-22** — **Admin Config test timestamp collision**. Back-to-back `Config::set()` calls in `ConfigFacadeTest` generated identical `effective_from` timestamps (< 1ms apart), preventing test from verifying version history correctly. **Challenge**: Test assertion failed (1 entry instead of 2). **Resolution**: Added `sleep(1)` between consecutive Config::set() calls to force distinct timestamps, allowing test to verify version history.

## Open Questions

- **2026-05-04** — Designer pipeline for the 6–10 Challenge scene plates: hire a designer, AI-generate-and-clean, or hybrid?
- **2026-05-04** — RP staking design remains open: pure RP units vs percentage of available RP with caps, plus tier/zone leaderboard treatment.
- **2026-05-04** — Potential Challenge lifecycle defaults remain open: issuer minimum RP/tier, 3-day response window, 4-day scheduling window, monthly callout cooldown, and result verification roles.
- **2026-05-04** — Headline highlight color on Challenge cards: always Kalaanba pink, or follow each challenger's club accent?
- **2026-05-20** — CODEOWNERS teams (`@rp-economy-owners`, `@architecture-council`, `@challenge-owners`, etc. per engine) need to be created on GitHub before reviewer-gating mechanism is real. When & how?
- **2026-05-20** — CI enforcement (Deptrac architecture lint, schema validation, Pest Architecture assertions) is named in Constitution and Build Plan but not yet wired. Belongs in Stage 0 Phase 0.2; when to implement?
- **2026-05-20** — Custom Pest test to count LOC per file for 400-line hard limit enforcement is referenced in engineering-standards §18 ("CI runs static checks: Deptrac, Pest Architecture, OpenAPI lint, file-length lint (a custom Pest test counting LOC per file)...") but not yet implemented. Timing?
- **2026-05-20** — Coverage threshold of 80% on domain code is asserted in engineering-standards §12 ("Coverage threshold: 80% lines on domain code") and incorporated into pipeline (Implementer must supply coverage report on Stage 6), but CI gate + tool setup (Pest coverage driver) not yet wired. When?
- **2026-05-22** — Where should `auth.allow_password_login` config key scope live — admin-governance registry with `scope: global`, or its own scope? Bootstrap endpoint (email+password) Phase 0.6 WP-A, OTP endpoint Phase 0.6 WP-B; config determines which method is enabled. Parked for WP-B design.

## Parked Ideas

- **2026-05-03** — A `journal-recap.prompt.md` user-level prompt to summarize the journal into a weekly digest.
- **2026-05-04** — Build the full Challenge Engine — lifecycle states, eligibility, buzz formula weights, RP values, anti-abuse, moderation. Full scoping in `docs/challenge-engine.md` and `docs/challenge-card-system.md`.
- **2026-05-04** — Further Challenge card system work is deferred for now.
- **2026-05-11** — Extract Notification, Analytics, and Buzz as standalone services once load testing reveals capacity limits on the monolith. Outlined in scaling path but deferred until partition strategy hits diminishing returns.
- **2026-05-12** — **Post-V1 deferrals** (explicit from brief §10.2): promotion/relegation, auto fixtures, paid competitions, referee marketplace, comments, fan voting, AI decisions, offline-first match entry.
- **2026-05-22** — **`EnsureSchemasCommand` (platform-wide schema creator)** not yet built. Phase 0.4 migration inlines `CREATE SCHEMA IF NOT EXISTS analytics` for self-sufficiency. Other engines' migrations will follow the same pattern until the platform command lands.
- **2026-05-22** — `src/components/ui/theme-toggle.tsx` (live folder) maintains its own ad-hoc storage using same `kalaanba-theme` key. When wired into live page, refactor to consume `useTheme()` for consistency.
- **2026-05-22** — When `src/components/_archive/` is fully deleted: drop `--kx-*` aliases from `globals.css`, `html.light` CSS selector, all `kx-*` keyframes (pulse, shimmer, pop-in, slide-\*, rise, toast-in, spin, progress, popover, check, aurora-a/b, mesh-a/b), archive theme-provider compat shim, and flip `next.config.ts: typescript.ignoreBuildErrors` back to false.

## Glossary

- **Sankofa** — The project's continuous documentation subagent; named after the Akan symbol meaning "go back and fetch it." Owns and maintains `docs/JOURNAL.md`.
- **CodeGenerator seam** — Pattern for testing code that depends on a final PHP class. Implemented via interface (e.g., `CodeGenerator`) with production binding (e.g., `RandomCodeGenerator` wrapping `Random\Randomizer`) and test binding (e.g., `StaticCodeGenerator`). Applicable across Kalaanba whenever wrapping `final` classes from PHP/Laravel.
- **Respect Points (RP)** — The non-monetary currency of the Challenge system; transferred between clubs based on call-out outcomes (win, draw, forfeit, ducked).
- **Fan Buzz** — A normalized hype score for a Challenge, mixing reactions, WhatsApp shares, predictions, comments, and view velocity. Displayed as a percentile against rolling-average buzz, not an absolute number.
- **Scene pack** — The hand-curated library of Challenge card background art plates (gauntlet, lightning, war-drums, etc.) the card composer draws from.
- **Work Packet (WP)** — Atomic unit of work that flows through all 10 Agent OS pipeline stages. Identified by `WP-YYYYMMDD-<slug>`. Every PR carries the WP ID and must show all 10 stage boxes ticked before merge. Example: `WP-20260520-configurable-draw-cost`. Within a larger phase (e.g., Phase 0.6), packets may be lettered sequentially: WP-A (identity foundation), WP-B (OTP + policies), WP-C (admin audit), etc.
- **Agent OS** — The governance meta-layer (10-stage pipeline, Constitution, chat modes, contracts, ADR cadence) that sits on top of Build Plan and describes _how_ every change is built (intake → release packet), orthogonal to _what & when_ (Build Plan's phasing).
- **AdminAuditEntry** — Readonly value object capturing actor_id, actor_role, request_id, route, method, path, response_status, payload_redacted (KEYS only, PII VALUES redacted), occurred_at. The canonical shape of an append-only audit log row; written only by `AdminAuditMiddleware`.
- **ApiError** — Frontend exception class wrapping the backend's standard error envelope (`{error: {code, message, details?, request_id?}}`). Stable `code` field (e.g. `auth.super_admin_only`) drives UI behavior; `message` is for display only. Falls back to `api.unknown_error` for non-conforming responses.
- **kx:next:** — Redis key prefix for Next.js Incremental Static Regeneration (ISR) cache entries. Used by cache-handler in production to namespace ISR state separately from other app caches.
- **themeBootstrapScript** — IIFE injected into `<head>` to set `data-theme` before hydration (FOUC fix). Reads localStorage (`kalaanba-theme` key) + `matchMedia("(prefers-color-scheme: light)")` before React hydration begins.
- **data-theme attribute** — Canonical theme switch on `<html>` element: `data-theme="dark"` (default, `:root` with `color-scheme: dark`) or `data-theme="light"` (`:root[data-theme="light"]`). Semantic alternative to class-based toggling.
- **`--kx-*` compat aliases** — Legacy CSS token aliases (e.g., `--kx-bg: var(--bg)`) kept only for archived components under `src/components/_archive/`; dropped once that folder is fully deleted.

## Action Items

- **2026-05-04** — User to drop the four supplied background images into `public/images/challenge-scenes/` as `bg-organic-neon.webp`, `bg-lightning-pink.webp`, `bg-red-geometric.webp`, `bg-adinkra-red.webp`. Then we wire them into the scene catalog.
- **2026-05-12** — Created `docs/Architecture/System_Architecture.md` as the authoritative baseline. Anchored to Full Kalaanba Brief.md and all 17 engine docs. Phasing mapped: Alpha/Beta/V1/Post-V1 each have concrete infra deltas.
- **2026-05-20** — Run canonical first test Work Packet end-to-end: _"Add configurable `challenge.draw.stood_ground_cost` value, admin-console-managed, High approval, default 5 Season RP"_ through all 10 pipeline stages to validate every gate & agent before scaling. Documented as suggested first run in `docs/AGENT_OS.md`.
- **2026-05-20** — Wire CI enforcement for the new craftsmanship standards: (1) custom Pest test that counts LOC per file and fails on > 400-line violations, (2) OpenAPI lint that validates `contracts/api/` specs, (3) Deptrac rules enforcing layering directives (domain framework-agnostic, repositories isolation, schema-per-module), (4) coverage gate that fails if domain code coverage < 80%. Belongs in Stage 0 Phase 0.2.
- **2026-05-13** — **Phase 0.2 continued (kalaanba-api)**: Create per-engine module skeleton folders (17 modules matching engine slugs: `app/Modules/Club/`, `app/Modules/RpEconomy/`, `app/Modules/MatchFixture/`, etc.). Each module: empty `Domain/`, `Application/`, `Infrastructure/`, `Http/` subdirs + registered `ServiceProvider` in `app/Providers/<Engine>ServiceProvider.php`. Register all 17 providers in `app/Providers/AppServiceProvider.php` via `Module::register(...)` helper. Deptrac rules + Pest Architecture tests remain green.
- **2026-05-20** — **Phase 0.2 final checkpoint (kalaanba-api)**: PostgreSQL auth configured (md5 for `kalaanba` role via pg_hba.conf); migrations run successfully (migrations, users, cache, jobs tables created in kalaanba_dev); `composer check` clean (pint 45 files PASS, larastan 0 errors, deptrac 0 violations, pest 6/6 tests pass). Build_Plan.md Phase 0.2 tasks marked complete with progress snapshot + changelog entry. Ready to advance to Phase 0.3 (event bus + outbox infrastructure).
- **2026-05-21** — **Phase 0.3 complete (kalaanba-api: event bus + outbox)**. Created: (1) `outbox_events` migration (append-only, relay polling index on `(delivered_at NULLS FIRST, occurred_at)`); (2) `event_dedupe` migration (composite PK `(event_id, listener_name)` for idempotent consumers); (3) `OutboxEnvelope` readonly DTO in `app/Support/EventBus/` enforcing `engine.action` regex in constructor; (4) `OutboxWriter` class (caller wraps in `DB::transaction()`, writer does NOT open one); (5) `RedisStreamPublisher` via predis v3, key convention `kalaanba.events.<engine>`; (6) `DedupeStore` with `isProcessed()` + `markProcessed()` using `event_dedupe` table; (7) `OutboxRelayCommand` (`php artisan outbox:relay --once/--limit/--sleep/--max-attempts`) polling & publishing with retry logic; (8) `HealthPingCommand` (`php artisan health:ping`) that writes `health.ping` event and verifies `delivered_at IS NOT NULL` in DB; (9) `config/eventbus.php` with `stream_prefix` and `max_relay_attempts` keys; (10) Added commands dir to `bootstrap/app.php`. Tests: 8 unit (OutboxEnvelope valid names + 5 rejection cases + schema_version + toPayloadArray), 3 arch (EventBus strict_types, OutboxEnvelope readonly, OutboxWriter transaction scope). `php artisan migrate` applied both migrations; `php artisan health:ping` confirmed delivery to Redis Streams. `composer check` green.
- **2026-05-22** — **Phase 0.4 complete (kalaanba-api: Analytics envelope)**. Created: (1) `analytics.events` table (partitioned by `occurred_at` with 7 forward-day + DEFAULT partition), (2) `analytics:ensure-partitions` command (rolls forward idempotently), (3) `EventSchemaRegistry` code-side catalogue keyed `<event_name>@v<schema_version>`, (4) `EventSchema` value object enforcing name regex `^[a-z][a-z0-9_]*\.[a-z][a-z0-9_]*$` and rejecting required/optional key overlap, (5) `SchemaCatalogue::all()` returning all registered schemas, (6) split event payload into `context` (environment) and `properties` (event-specific) both jsonb, (7) validation gate on `properties` only. Architecture test gates catalogue drift. Quality: pint, larastan, deptrac all green; 38 Pest tests passing. Ready to advance to Phase 0.5 (Admin Config registry).
- **2026-05-22** — **Phase 0.5 complete (kalaanba-api: Admin Config registry)**. Created: (1) `admin_config` table with columns `key` (string), `scope` (platform|season|hub|venue), `scope_id` (nullable string(36) to handle nullable UUIDs in Postgres), `value` (jsonb), `effective_from` (timestamp), `version` (auto-increment per key+scope+scope_id), UNIQUE constraint on (key, scope, scope_id, effective_from, version); (2) `PostgresConfigRepository::get(key, scope, scopeId, at?)` with time-travel support (returns most recent row where effective_from <= at), uses `version DESC` in ORDER BY to disambiguate rows with identical effective_from timestamps; (3) `Config` facade with versioning strategy (both effective_from and version used for ordering); (4) Redis cache with pattern-based invalidation `del(kx:config:v1:<key>:<scope>:<scopeId>:*)` on every set(); (5) Test isolation: `$seed=true` in TestCase base class auto-runs seeders for Feature tests, ensures default config values available; sleep(1) between consecutive Config::set() calls to prevent timestamp race conditions. Architecture test gates config reads outside domain+config modules. Quality: pint, larastan, deptrac all green; 14 Pest tests passing. **All 4 quality gates exit code 0: Pint ✅, Larastan ✅, Deptrac ✅, Pest ✅ (52 tests total: 38 Phase 0.4 + 14 Phase 0.5).** Build_Plan.md Phase 0.4 & 0.5 tasks marked complete. Ready to advance to Phase 0.6 (next).
- **2026-05-22** — **Phase 0.4 & 0.5 checkpoint (kalaanba-api, Stage 0 Foundations)**: Foundation layers operational — analytics event partitioning + schema registry (Phase 0.4) + Admin Config versioning (Phase 0.5) complete with 52 passing tests. All quality gates green (Pint, Larastan, Deptrac, Pest) for 15 working days. Ready to advance: consult Build_Plan.md Phase 0.6 scope and decide next priority (likely per-engine module scaffolding or authentication/authorization layer).
- **2026-05-22** — **Phase 0.6 WP-A complete (kalaanba-api: Identity Foundation)**. Landed `WP-20260522-identity-foundation`: (1) Installed Laravel Sanctum v4.3.2; published config + personal_access_tokens migration. (2) Extended users table: `role` (varchar(32), CHECK constraint, backing `Kalaanba\Support\Auth\Role` enum with 11 canonical roles in snake_case: fan, player, club_rep, club_admin, comp_org, referee, officiator, facility_mgr, hub_admin, kalaanba_admin, super_admin); `phone_e164_hash` (unique); `phone_e164_last4` (last 4 digits, PII rule); `archived_at` (archive-don't-delete per Constitution Law 13); `last_seen_at`. (3) Added `IdempotencyKeyMiddleware`: rejects writes missing `Idempotency-Key` header (error code `auth.idempotency_key_required`), dedupes on 24h Redis TTL (`kx:idem:v1:` prefix), replays with `meta.idempotent_replay: true`. (4) Registered `auth` rate limiter: 5/min keyed on email+IP. (5) Exposed `/api/v1/auth/sessions` (POST, issue token with 30-day expiry) and `/api/v1/auth/sessions/current` (DELETE, revoke token). Bootstrap-only email+password; OTP endpoint from WP-B behind `auth.allow_password_login` config. (6) OpenAPI 3.1 contracts written for both endpoints at `contracts/api/auth/`. (7) Added 3 architecture tests: Role enum framework-agnostic; IdempotencyKey middleware confined to Support; engine modules do NOT depend on `App\Models\User` directly. (8) Updated Build_Plan.md Phase 0.6 snapshot. Quality: `composer check` exit 0 — Pint (65 files), Larastan level 6 (0 errors), Deptrac (0 violations), Pest (65 tests, 141 assertions). WP-A ready for Stage 7–10 review.
- **2026-05-22** — **Phase 0.6 WP-B complete (kalaanba-api: OTP issuance + Policies + Scope middleware).** Landed `WP-20260522-otp-and-policies`: (1) Implemented `CodeGenerator` interface seam + `RandomCodeGenerator` (production, wraps `Random\Randomizer`) + `StaticCodeGenerator` (test double). (2) Phone PII via `PhoneHash` (HMAC-SHA256); DB only sees `phone_e164_hash` + `phone_e164_last4`. (3) OTP machinery confined to `Support\Auth\Otp` + HTTP layer; no engine modules access. (4) `ScopeResolver` port with `DenyAllScopeResolver` default (only platform admins); each engine binds concrete resolver; fail-safe by default. (5) Config keys (5): `auth.otp_ttl_seconds` (300, medium), `auth.otp_max_attempts` (5, medium), `auth.otp_length` (6, high), `auth.otp_provider` (mock, high; allowed: mock, whatsapp), `auth.allow_password_login` (true, critical — kill switch for password fallback). (6) Dual login paths: `POST /api/v1/auth/sessions` (password) + `POST /api/v1/auth/otp/request` + `POST /api/v1/auth/otp/verify` (OTP); password gated by runtime config. (7) Single Idempotency-Key dedupe path (`kx:idem:v1:`) for both `otp/request` and `otp/verify`. (8) Policies scaffold + scope middleware complete. Quality: `composer check` exit 0 — Pint, Larastan, Deptrac, Pest all passing. WP-B marked complete in Build_Plan.md Phase 0.6. Ready for Stage 7–10 review. **Next: WP-C — `admin.admin_audit_log` table + `AdminAuditMiddleware` (auto-log every authenticated write where actor is platform admin, append-only, partitioned monthly, WP-20260522-admin-audit-log).**
- **2026-05-22** — **Ops task: Admin audit log partition rotation cron.** Implement monthly partition rotation: create forward-day partition for next month, detach 7-year-old months for cold archive to R2. Ensure backward-compatibility with DEFAULT catch-all partition.
- **2026-05-22** — **Deploy task: RBAC lock on admin_audit_log.** Append `REVOKE UPDATE, DELETE ON admin_audit_log FROM <app_role>` to `scripts/setup-postgres.sql` so prod app role is structurally blocked from mutating audit rows (defense-in-depth beyond middleware).
- **2026-05-22** — **Frontend skeleton (kalaanba-front Phase 0.7 WP-20260522-frontend-skeleton) complete**. Archived legacy showcase. Wired API client (`src/lib/api/{envelope,client,index}.ts`) with `ApiError`, token storage, Zod validation, Idempotency-Key auto-attach. TanStack Query `AppProviders` with 60s staleTime, retry false for ApiError. Reverb/Echo client deferred stub. OpenAPI codegen pipeline via `scripts/codegen-api.mjs`. Next.js: standalone output, ISR cache via redis-strings handler (redis@^4 pinned), PM2 cluster config. TS strict + `noUncheckedIndexedAccess` enabled. 8 vitest tests all green. Pipeline: npm run lint ✅, npm run typecheck ✅ (0 errors), npm run test ✅ (8/8 pass, 794ms), npm run build ✅ (standalone, 4 prerendered). Ready for Stage 7–10 review. **Next: per-engine backend module scaffolding (Phase 0.7 kalaanba-api) or continue kalaanba-front with identity/auth UI.**
- **2026-05-22** — **DEFERRED (Stage 4 Distribution)**: Wire real Reverb credentials into `getEcho()` (currently returns null stub). Blocking: NEXT_PUBLIC_REVERB_APP_KEY + NEXT_PUBLIC_REVERB_HOST not yet available.
- **2026-05-22** — **DEFERRED (Ops task)**: Install Redis on the Next.js node before flipping `NODE_ENV=production` (cache-handler routes ISR writes to Redis).
- **2026-05-22** — **DEFERRED (CI task)**: Add `npm run codegen` step to CI once `contracts/api/*` grows beyond admin audit-log endpoint.
- **2026-05-22** — **WP-20260522-theme-rebuild complete (kalaanba-front Phase 0.7 bonus).** Rewrote theme system end-to-end: (1) New semantic token names (`--bg`, `--surface`, `--warning`, `--danger`, etc.; dropped `--kx-*` prefix). (2) `data-theme` attribute on `<html>` as canonical switch (dark/light). (3) `ThemeProvider` uses `useSyncExternalStore` + exports `themeBootstrapScript` IIFE (prevents FOUC). (4) Backward-compat preserved: `--kx-*` aliases + `html.light` + all `kx-*` keyframes for archive. (5) Adopted framer-motion for component animations. (6) Restored original landing/showcase at `/legacy/landing` + `/legacy/showcase` from commit `ecc7aec`. (7) Fixed routing: renamed `(legacy)` route group to real `src/app/legacy/` directory. New `theme-provider.test.tsx` (3 tests). Quality: lint ✅, typecheck ✅ (0 errors), vitest 11/11, build standalone OK. Ready for Stage 7–10 review.
- **2026-05-22** — **FUTURE CLEANUP**: Once archive paths (`_archive/`, `(legacy)/`) are no longer needed, flip `typescript.ignoreBuildErrors: false` back in next.config.ts to re-enable build-time typecheck on full codebase.
- **2026-05-22** — Theme tokens dropped `--kx-*` prefix in favor of plain semantic names: `--bg`, `--surface`, `--surface-2`, `--fg`, `--fg-muted`, `--warning #f59e0b` (amber, disambiguates from pink), `--danger #ef4444` (red, disambiguates from both), `--divider` (always subtle), `--border`, `--border-strong`, `--ring`, `--shadow-{sm,md,lg,pink}`, `--radius-{pill,button,card,card-lg,control}`. Reason: engineering-standards §2 (one name per concept); visually separate warning/danger from brand pink.
- **2026-05-22** — Theme provider refactored: switched from useState-in-effect to `useSyncExternalStore` (React-blessed pattern for external state) after eslint rejected set-state-in-effect anti-pattern. `setTheme()` writes localStorage + dispatches custom `kalaanba-theme-change` event. Exports `themeBootstrapScript` — IIFE in `<head>` that reads localStorage + `matchMedia("(prefers-color-scheme: light)")` and sets `data-theme` **before hydration** (FOUC fix).
- **2026-05-22** — Canonical theme switch is `data-theme="dark"|"light"` attribute on `<html>` (semantic over class-based). Dark = default at `:root` with `color-scheme: dark`; light at `:root[data-theme="light"]`. Preserved backward-compat: `--kx-*` aliases, `html.light` selector, all `kx-*` keyframes (pulse, shimmer, pop-in, slide-\*, rise, toast-in, spin, progress, popover, check, aurora-a/b, mesh-a/b) remain for archived components until deletion.
- **2026-05-22** — Adopted framer-motion for component animations going forward; CSS keyframes retained for ambient/decorative surfaces.
- **2026-05-22** — Restored original landing at `/legacy/landing` and prototype showcase at `/legacy/showcase` from git commit `ecc7aec:src/app/page.tsx`. Renamed route from `(legacy)` (group, no URL segment) to real `src/app/legacy/` directory; updated tsconfig + eslint excludes. New `/legacy` index with card navigation.
- **2026-05-22** — **Phase 0.6 WP-C complete (kalaanba-api: Admin audit log).** Landed `WP-20260522-admin-audit-log`: (1) Created `admin_audit_log` table (append-only, partitioned monthly by `occurred_at` with DEFAULT catch-all partition). (2) `AdminAuditMiddleware` auto-logs all authenticated writes (added to `api` middleware stack) where actor_role ∈ [kalaanba_admin, super_admin]; writes occur in same request lifecycle (not async). (3) `RequireSuperAdminMiddleware` + `Role::isSuperAdmin()` gate read endpoint `GET /api/v1/admin/audit-log` (error code `auth.super_admin_only`). (4) `PayloadRedactor` strips VALUES (preserves KEYS) for: password, token, secret, otp, authorization, cookie, api_key, apikey, pin, cvv, phone_e164 (case-insensitive substring match). (5) Reader endpoint uses opaque base64 cursor `{occurred_at_iso}|{id}` with default limit 25, hard cap 100. (6) `actor_id` field typed `varchar(64)` (legacy bigint users, future UUID post-migration). (7) New contracts: `contracts/api/admin/get-audit-log.v1.yaml` + `contracts/config/admin/admin.audit_log_retention_days.yaml` (default 2555 days ~7 years, critical tier). (8) Arch test: audit machinery confined to Support + App layers (domain forbidden). (9) Quality: 98 tests / 237 assertions all passing; `composer check` exit 0 — Pint, Larastan, Deptrac, Pest all green. Ready for Stage 7–10 review and Phase 0.7 transition.
- **2026-05-22** — **Challenge & resolution (frontend cache handler)**: Pinned `redis@^4` because v5+ broke @neshca's CJS interop. Error: "Named export 'commandOptions' not found". Reason: @neshca uses require() + destructure pattern that v5's ESM-first approach breaks. Resolution: locked to v4 in package.json; zero production friction (same Redis API).

key: zone.area_suggestion_max_name_length
engine: zone
scope: platform
value_type: integer
default: "80"
approval_level: low
description: |
  Maximum length (after trimming) of `proposed_name` accepted on
  `zone.area_suggested`. Validation happens at the API boundary.

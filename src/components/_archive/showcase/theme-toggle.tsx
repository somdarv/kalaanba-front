"use client";

/**
 * Inert stub — the runtime theme switcher was removed. This component
 * renders nothing so archived showcase imports continue to resolve
 * without producing any UI. Do NOT use in new code.
 */
export function KxThemeToggle() {
  return null;
}
